import React from 'react';


/**
 * Renders the removal reasons for the rules and removal reasons page.
 * @return {JSX.Element} The rendered component.
 */
export function RemovalReasons() {
    return (
        <div data-testid="remov-reasons">
            Removal Reasons
        </div>
    );
}
